[package]
name = "vault"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "=22.0.1"

[dev-dependencies]
soroban-sdk = { version = "=22.0.1", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
