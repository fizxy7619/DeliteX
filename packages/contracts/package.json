{
  "name": "@delitex/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:vault": "cargo build -p vault --target wasm32-unknown-unknown --release",
    "build:router": "cargo build -p router --target wasm32-unknown-unknown --release",
    "build": "pnpm run build:vault && pnpm run build:router",
    "test": "cargo test"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "wasm-opt": "^1.4.0"
  }
}
